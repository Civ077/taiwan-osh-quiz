rules_version = '2';
// 這個專案沒有用到 Firestore（排行榜在 Realtime Database）。理由同 storage.rules：
// 預設規則配上匿名登入等於對外開放，先寫好擺著，啟用當下就能鎖死。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
